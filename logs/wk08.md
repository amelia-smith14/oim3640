# Week 8 (3/10 + 3/12)

## What I learned today
On Tuesday, we reviewed strings and lists as well as some of their differences and similarities. For example, strings are immutable but lists are not. We then moved on to dictionaries, looking a items like list and dict, and thinking about how we might be able to implement them within our first mini project. On Thursday, we reviewed the work we did with dictionaries and introduced the method of get to count letters in words. From there, we moved onto solving problems in Python Challenge.

## Code/work I'm proud of (optional)
def histogram(s):
    d = {}
    for c in s:
        if c in d:
            d[c] += 1
        else:
            d[c] = 1
    return d

print(histogram(data))

This takes a string and counts how many times each letter shows up and returns a dictionary with the letter and the associated count as the value.

## Challenges I faced
The concepts of dictionaries were a bit confusing for me when I first attempted them, especially with all of the different symbols.

## AI usage (if any)
I have been using the GitHub Copilot more for coding and adjusting to that workflow.

## Questions for next time
None for this week.