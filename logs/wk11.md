# Week 11 (4/7 + 4/9)

## What I learned today
On Tuesday, we began with a review and some questions about APIs. We then moved on to how to read and write files, and the differences between file types like JSON or CSV and how they are brought into Python. We also looked at YAML and SQL, then discussed our second mini projects in groups. On Thursday, we began with web apps, talkking about Flask and how we can use it to create websites. We reviewed URLs and how they map routes to different places in the app. We also learned how we can use HTML files to make the sites look nicer, and how to integrate some APIs we had used before like Yahoo Finance.

## Code/work I'm proud of (optional)
from flask import Flask
app = Flask(__name__)

@app.route('/')
def hello_world():
    return 'Hello, World!'

@app.route('/hello/<name>')
def hello_name(name):
    return f'Hello, {name}!'

@app.route('/square/<int:number>')
def square_number(number):
    return f'{number} squared is {number ** 2}.'

if __name__ == '__main__':
    app.run(debug=True)

This code is a simple web app that creates 3 different routes that can say Hello world, hello to the user's name, or square a number.

## Challenges I faced
I am struggling a bit with how to get the HTML files to appear in my browser and the correct way to set up the folder.

## AI usage (if any)
I used the GitHub copilot to help me write a lot of the code for my web app, like the functions and route assignments.

## Questions for next time
[What remains unclear?]